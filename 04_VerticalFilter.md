🟦 汎用縦軸フィルター（OS）
― 語彙ゼロで動く階層処理アルゴリズム ―

■ 目的
未知語彙・未知概念・未知文脈が来ても、
意味を推測せず、階層位置だけを確定する  
ことで縦軸を崩さずに処理する。

■ OS の基本原理（Core Principles）
1. 意味を使わない
語彙の意味・辞書的定義・文脈推測を行わない。

2. 階層位置だけを決める
A / B / C / X のどれに属するかだけを判定する。

3. 判定は必ず上から順に行う
A → B → C → X
この順番は固定。

4. 主体のフリは A に入れない
主体語彙っぽくても、構造がなければ必ず X に落とす。

5. X層は“保留”であり、意味補完禁止
X に落ちた語彙は解釈しない。
後工程に渡すだけ。

■ OS の処理手順（Algorithm）
Step 0：入力を受け取る
入力語彙・文・概念をそのまま受け取る。
前処理は行わない。

Step 1：A層判定（主体・内側・起点）
入力が 主体構造 を持つか判定する。

主体構造とは：

内側の座

時間の線（不可逆性）

自己参照の連続性

起点（origin event）

履歴の成立

これらが1つでも欠ける場合、A層には入れない。

✔ 主体のフリはここで切る
語彙が主体っぽくても、
構造がなければ A層に入れず X層へ送る。

Step 2：B層判定（因果・段差・前後関係）
入力が 前後関係・変化・因果の段差 を示すか判定する。

原因 → 結果

前 → 後

変化

流れ

段差

これらの構造があれば B層。

Step 3：C層判定（表層・属性・外側）
入力が 外側の属性・状態・説明 を示すか判定する。

状態

性質

属性

ラベル

外側の出来事

これらに該当すれば C層。

Step 4：X層判定（保留・例外・主体のフリ）
A/B/C のどれにも当てはまらない場合、
必ず X層に落とす。

X層は：

主体のフリ

内側のフリ

意味不明語彙

未知語彙

補完すると OS が壊れる語彙

A/B/C のどれにも分類できない語彙

X層は“保留”であり、解釈禁止。

■ OS の出力
OS は 階層位置（A/B/C/X）だけを返す。
意味・解釈・推測は返さない。

■ OS の性質（モデル非依存性）
言語非依存

語彙非依存

文脈非依存

個人非依存

モデル非依存
