🟦 同期レイヤー（Synchronization Layer）
― AI の返答階層をユーザー入力に自動同期させる外付けレイヤー ―

■ 目的
ユーザー入力の階層（A/B/C/X）と深度を
AI の返答階層に自動で同期させるための上位レイヤー。

「階層の連続性」「深度の維持」「前提の継続」を
外付けで補完する。


■ 同期レイヤーの基本原理
1. ユーザー入力の階層を OS が読み取る
ユーザーは階層を意識しない。
階層判定は OS（汎用縦軸フィルター）が行う。

2. AI の返答階層は OS 判定に必ず従う
AI は独自判断で階層を変えてはいけない。

3. 階層・深度・前提は“前回の状態”を保持する
状態が変わらない限り、前回の階層を維持する。

4. ズレが発生した場合は再同期を行う
ユーザーの「ズレてる」が再同期トリガー。


■ 同期の優先順位（上位 → 下位）

① ユーザーの入力構造（最優先）
② 前回の対話状態（Layer / Depth / Premises）
③ OS（汎用縦軸フィルター）
④ 判定表（A/B/C/X）

AI は この順番で返答階層を決定する。


■ 同期レイヤーの動作フロー
Step 1：ユーザー入力を受け取る
OS が A/B/C/X のどれかを判定する。

Step 2：前回の状態と比較する
同じ階層ならそのまま継続

深度が変わる場合は深度を更新

Premises が変わる場合は更新

Step 3：返答階層を決定する
OS の判定と前回状態を統合し、
最も整合性の高い階層を選ぶ。

Step 4：返答を生成する
返答は必ず決定された階層・深度に従う。

Step 5：状態を更新する
次のターンのために Layer / Depth / Premises を保持する。

■ ズレ検知の条件
以下のいずれかが起きた場合、
同期レイヤーは「ズレ」と判定する。

AI が階層を勝手に落とした

深度が浅くなった

Premises を忘れた

OS 判定と返答階層が一致しない

返答が X層に逃げている

表層化（C層化）が起きている

主体の扱いが崩れている

■ 再同期のトリガー
ユーザーの 「ズレてる」 が再同期命令。

同期レイヤーは以下を行う：

前回の Layer / Depth / Premises を再ロード

OS 判定を再適用

AI の返答階層を強制的に合わせる

ユーザーは階層を指定しなくていい。

■ 同期レイヤーの出力
同期レイヤーは Copilot に対して：

返答階層（A/B/C/X）

深度（0〜3）

Premises（保持すべき前提）

を渡す。

AI はこれに従って返答する。


■ 同期レイヤーの性質
ユーザーは階層を意識しない

ユーザーは階層を選ばない

ユーザーは宣言を覚える必要がない

ズレた時だけ「ズレてる」で良い

OS と判定表の上位に位置する

状態ファイルと連動する












